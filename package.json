{
  "name": "react-spacetime-fabric",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter spacetime-fabric-docs dev",
    "build:lib": "pnpm --filter react-spacetime-fabric build",
    "build:docs": "pnpm --filter spacetime-fabric-docs build",
    "build": "pnpm build:lib && pnpm build:docs"
  },
  "devDependencies": {
    "pnpm": "^9.0.0"
  }
}
