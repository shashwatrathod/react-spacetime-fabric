{
  "name": "react-spacetime-fabric",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter docs-website dev",
    "build:lib": "pnpm --filter react-spacetime-fabric build",
    "build:docs": "pnpm --filter docs-website build",
    "build": "pnpm build:lib && pnpm build:docs"
  },
  "devDependencies": {
    "pnpm": "^9.0.0"
  }
}
